import SwiftUI

// Settings view is no longer used — API key is hardcoded and profile is in ProfileTab
// Keeping this file to avoid removing it from the project reference
struct SettingsView: View {
    var body: some View { EmptyView() }
}
